# 贡献指南

> 这是一天制比赛，节奏优先于流程。规范的目的是防冲突，不是增加负担。

## 分支命名规范

| 类型 | 格式 | 示例 |
|------|------|------|
| 主分支 | `main` | 仅存放可交付代码 |
| 功能分支 | `feature/<简短名称>` | `feature/data-augmentation` |
| 修复分支 | `fix/<简短名称>` | `fix/label-format-error` |

## Commit Message 格式

```
<类型>: <简短描述>
```

| 类型 | 使用场景 |
|------|----------|
| `feat` | 新增功能 |
| `fix` | 修复 Bug |
| `chore` | 配置、依赖、文档等 |

**示例：**
```
feat: 添加数据增强脚本
fix: 修复标注格式转换错误
chore: 更新 requirements.txt
```

## 合并流程

**自己的模块 → 直接合：**
改的是自己负责的文件，推上去转头说一声，自己点 Merge，不用开 PR。

**改了别人的模块 → 开 PR：**
推分支，喊对应负责人看一眼，确认后合并。

比赛结束前合不进 main 才是真正的风险，**速度优先**。

## 大文件管理规范

- **模型权重（*.pt 等）禁止提交到 git**
- **数据集禁止提交到 git**（主办方下发后 U 盘拷贝给每人）
- **训练输出（runs/）禁止提交**（已在 .gitignore 屏蔽）
- 最终 `best.pt` 直接提交给主办方，不进 git

## 新手禁止事项

- **禁止直接在 `main` 上开发** —— 必须在 `feature/` 或 `fix/` 分支上工作
- **禁止 force push**（`git push --force`）到任何共享分支
